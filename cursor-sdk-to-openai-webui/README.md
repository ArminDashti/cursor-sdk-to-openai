# cursor-sdk-to-openai-webui

Read-only admin UI: **logs** and **endpoint reference** only (no chat).

## Run

```bash
npm install
npm run dev
```

Open http://127.0.0.1:5190 — sign in with `armin` / `dopadopa123`.

Set `VITE_API_BASE` if the API is not on `http://127.0.0.1:8140`.
